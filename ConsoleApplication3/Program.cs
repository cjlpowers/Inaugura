using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;


namespace ConsoleApplication3
{
    class Program
    {
        static void Main(string[] args)
        {
            //SandBox sandbox = new SandBox();
            //for (int i = 0; i < 10; i++)
            //    sandbox.Add(new Ant(100));
            //for (int i = 0; i < 26; i++)
            //    sandbox.Epoch();

            //sandbox.Write("Env.xml");

            //System.Xml.XmlTextReader reader = new System.Xml.XmlTextReader(System.IO.File.OpenRead("Env.xml"));

            //reader.MoveToContent();

            //SandBox env2 = Inaugura.Xml.Helper.Load<SandBox>(reader);
            //object obj = env2;
        }
    }
}
